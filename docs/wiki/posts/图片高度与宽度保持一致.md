
宽度为百分比时，高度与宽度保持一致，需要动态设置。

```js
window.onload = function () {
  var videoImg = document.querySelectorAll('.video-item .video-img');
  for (let i = 0; i < videoImg.length; i++) {
    videoImg[i].style.height = videoImg[i].offsetWidth + 'px'
  }
}
```